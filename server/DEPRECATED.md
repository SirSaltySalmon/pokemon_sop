# Deprecated

The app now runs on **Next.js**. API routes live under `app/api/`. Do not run `server/server.js` for production.

The `server/routes/` logic was ported to Next Route Handlers (`lib/character-api.ts`, `app/api/**`).
